using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NFCInputManager : MonoBehaviour
{
    [SerializeField] private BreadDate bradData;
    [SerializeField] private BTSerialManager _btSerialManager;

    private string _uid;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void SetBread(int number)
    {

    }

}
